# 📘 Assignment: Build REST APIs with RapidAPI

## 🎯 Objective

Learn how to integrate and consume REST APIs using the RapidAPI platform. You'll build applications that fetch data from multiple public APIs, handle responses, and display information in a meaningful way.

## 📝 Tasks

### 🛠️ Explore and Integrate RapidAPI

#### Description
Sign up for a RapidAPI account, explore the API marketplace, and choose at least two public APIs to integrate into your application. Learn how to authenticate requests and handle API responses properly.

#### Requirements
Completed program should:

- Successfully authenticate with RapidAPI using API keys
- Make requests to at least two different REST APIs
- Handle API responses and parse JSON data correctly
- Implement error handling for failed requests

### 🛠️ Build a Data Display Application

#### Description
Create an application that fetches data from your chosen APIs and displays the information in a user-friendly format. The application should allow users to search or filter data based on different criteria.

#### Requirements
Completed program should:

- Accept user input to query the APIs
- Fetch and display results from multiple API sources
- Format and present data clearly (tables, lists, or structured output)
- Include proper error messages for invalid inputs or API failures
